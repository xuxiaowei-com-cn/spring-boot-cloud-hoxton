#
# Nacos-开发配置
spring:
  cloud:
    nacos:
      config:
        file-extension: yml
        server-addr: localhost:10121
      discovery:
        server-addr: localhost:10121
        metadata:
          # 灰度发布
          gray: false
