#
# Nacos-开发配置
spring:
  cloud:
    nacos:
      discovery:
        server-addr: localhost:10121
      config:
        file-extension: yml
        server-addr: ${spring.cloud.nacos.discovery.server-addr}
