#
# Nacos-开发配置
spring:
  cloud:
    nacos:
      discovery:
        server-addr: localhost:10101
      config:
        file-extension: yml
        server-addr: ${spring.cloud.nacos.discovery.server-addr}
